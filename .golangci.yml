version: "2"

run:
  timeout: 2m

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - ineffassign
    - revive

issues:
  exclude-rules:
    - path: _test\.go$
      linters:
        - errcheck